import 'package:flutter/material.dart';

class VoicePage2 extends StatelessWidget {
  const VoicePage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: const Text('Voice 2'),
      ),
    );
  }
}
