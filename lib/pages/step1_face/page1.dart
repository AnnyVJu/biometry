import 'package:flutter/material.dart';

class FacePage1 extends StatelessWidget {
  const FacePage1({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: const Text('Face'),
      ),
    );
  }
}
