import 'package:flutter/material.dart';

class FacePage2 extends StatelessWidget {
  const FacePage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: const Text('Face bio page 2'),
      ),
    );
  }
}