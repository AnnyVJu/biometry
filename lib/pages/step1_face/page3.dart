import 'package:flutter/material.dart';

class FacePage3 extends StatelessWidget {
  const FacePage3({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Face bio page 3'),
    );
  }
}